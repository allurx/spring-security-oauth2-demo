server:
  port: 9000
spring:
  security:
    oauth2:
      client:
        registration:
          github:
            clientId: github-client-id
            clientSecret: github-client-secret
logging:
  level:
    org.springframework.security: debug
